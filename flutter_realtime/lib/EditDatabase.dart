import 'package:flutter/material.dart';

class EditDatabase extends StatelessWidget {
  const EditDatabase({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
